---
phase: 6
plan: 3
wave: 3
---

# Plan 6.3: Mechanic KPI View & Build Verification

## Objective
Add a personal KPI summary section to the mechanic's dashboard so they can track their own performance metrics. Verify the entire Phase 6 builds cleanly.

## Context
- KPI aggregation logic already exists in MungkhudShop's `mechanic-kpi.js` (Phase 5).
- This plan adapts the same aggregation for the Management Portal's mechanic page, scoped to the logged-in mechanic only.

## Tasks

<task type="auto">
  <name>Add personal KPI stats to mechanic page</name>
  <files>
    Management/src/pages/mechanic/index.html
    Management/src/pages/mechanic/logic.js
  </files>
  <action>
    - Add a "ผลงานของฉัน" (My Performance) section below the active jobs.
    - Fetch closed jobs from this month where the mechanic was lead or helper.
    - **KPI visibility is role-gated:**
      - **All roles (mechanic, sa, etc.):** Show only:
        - Jobs Led (count)
        - Jobs Assisted (count)
      - **Manager / Owner / Admin only:** Additionally show:
        - Total Hours Worked (from `work_duration_minutes`)
        - Revenue Generated (sum of `grand_total` from led closed jobs)
        - Total Jobs Completed (closed count)
    - Implement role check: `const canSeeFinancials = ['manager', 'owner', 'admin'].includes(currentUser.role)`.
    - Conditionally render the financial/sensitive KPI cards inside a wrapper that is hidden if the user is a plain mechanic or SA.
    - Use the Management Portal's glassmorphism card styling for consistency.
  </action>
  <verify>Check that KPI stat elements exist and financial cards are conditionally rendered.</verify>
  <done>Mechanics see only led/assisted counts. Managers/owners/admins see full financial and hours KPIs.</done>
</task>

<task type="auto">
  <name>Build verification</name>
  <files>
    Management/vite.config.js
  </files>
  <action>
    - Run `npm run build` in the Management directory.
    - Verify no import errors, no missing files, no syntax errors.
    - Confirm the mechanic page appears in the output bundle.
  </action>
  <verify>Run `npm run build` and check for errors.</verify>
  <done>Management Portal builds cleanly with the new mechanic page.</done>
</task>

## Success Criteria
- [ ] Mechanic sees only **jobs led** and **jobs assisted** for the current month.
- [ ] Manager / Owner / Admin also see **hours**, **revenue**, and **total completed jobs**.
- [ ] Role check uses `currentUser.role` — no financial data leaks to mechanic/SA role.
- [ ] `npm run build` succeeds for the Management project.
