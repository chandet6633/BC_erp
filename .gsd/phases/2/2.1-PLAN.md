---
phase: 2
plan: 1
wave: 1
---

# Plan 2.1: Test Job Workflow, Kanban & QC

## Objective
Write comprehensive Playwright E2E tests for the core job workflow in MungkhudShop. This includes job creation, Kanban tracking, and Quality Control. Log any failures instead of stopping to fix them.

## Context
- `webapp/tests/e2e/specs/mungkhud-job-flow.spec.js`
- `webapp/MungkhudShop/src/pages/job.js`
- `webapp/MungkhudShop/src/pages/kanban.js`

## Tasks

<task type="auto">
  <name>Test Job Creation Flow</name>
  <files>
    tests/e2e/specs/mungkhud-job-flow.spec.js
  </files>
  <action>
    - Create a test file for the job workflow.
    - Write a test to log in using the `auth.js` utility, navigate to the Job Creation page, and attempt to create a new job with an ad-hoc line item and an assigned mechanic.
    - Write a test to ensure duplicate plate warnings work.
    - Use `try/catch` or Playwright's soft assertions if possible so failures are reported but the test suite can continue.
  </action>
  <verify>Run `npx playwright test specs/mungkhud-job-flow.spec.js --project=MungkhudShop`</verify>
  <done>Job creation tests are written and executed.</done>
</task>

<task type="auto">
  <name>Test Kanban & QC Flow</name>
  <files>
    tests/e2e/specs/mungkhud-kanban.spec.js
  </files>
  <action>
    - Write a test to navigate to the Kanban board and verify that active jobs appear.
    - Test the job timer (starting/stopping a job).
    - Test the QC upload modal opening and attempting to upload an image.
  </action>
  <verify>Run `npx playwright test specs/mungkhud-kanban.spec.js --project=MungkhudShop`</verify>
  <done>Kanban tests are written and executed.</done>
</task>

## Success Criteria
- [ ] Core job and kanban tests are written.
- [ ] Any failures are caught by the test runner for later aggregation.
