---
phase: 4
plan: 1
wave: 1
---

# Plan 4.1: Fix E2E Test Authentication

## Objective
Fix the systemic login failures identified in Phases 2 and 3 so the comprehensive E2E test suites can successfully authenticate and navigate to their respective dashboards.

## Context
- `webapp/tests/e2e/utils/auth.js`
- `webapp/scripts/` (Seed scripts)
- `.gsd/phases/2/MungkhudShop-Bugs.md`
- `.gsd/phases/3/Portal-Bugs.md`

## Tasks

<task type="auto">
  <name>Seed Test Accounts</name>
  <files>
    webapp/scripts/seed-test-users.mjs
  </files>
  <action>
    - Ensure the local NocoDB `users` and `employees` tables contain standard testing accounts: `admin`, `manager`, `sa`, and `mechanic`.
    - If a seed script doesn't exist for these users, create and execute one.
  </action>
  <verify>Verify NocoDB has the necessary accounts configured for login.</verify>
  <done>Test users exist in NocoDB.</done>
</task>

<task type="auto">
  <name>Patch auth.js login helpers</name>
  <files>
    tests/e2e/utils/auth.js
  </files>
  <action>
    - Update `loginMungkhudShop` to use the correct credentials and wait for the correct post-login URL or UI state.
    - Update `loginPortal` to use the correct credentials and wait for the correct post-login URL or UI state (it currently times out waiting for `**/dashboard/index.html*`).
  </action>
  <verify>Run `npx playwright test specs/health.spec.js` (if it includes login checks) or a specific role test to verify the hook no longer times out.</verify>
  <done>Tests can successfully log in.</done>
</task>

## Success Criteria
- [ ] Test accounts exist in the mock database.
- [ ] `auth.js` accurately mimics the login flow without timing out.
- [ ] The E2E suites can be run without failing in the `beforeEach` hook.
