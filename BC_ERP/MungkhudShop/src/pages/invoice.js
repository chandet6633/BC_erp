import { createDocumentPage } from './document-factory.js'
export const initInvoicePage = createDocumentPage({ title: 'ใบแจ้งหนี้ / ใบกำกับภาษี', icon: 'receipt_long', prefix: 'IV', collection: 'invoices' })
