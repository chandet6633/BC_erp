import { createDocumentPage } from './document-factory.js'
export const initCreditNotePage = createDocumentPage({ title: 'ใบลดหนี้ (CN)', icon: 'note_alt', prefix: 'CN', collection: 'credit_notes' })
