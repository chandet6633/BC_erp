import { createDocumentPage } from './document-factory.js'
export const initRequisitionPage = createDocumentPage({ title: 'ใบเบิก (RQ)', icon: 'assignment', prefix: 'RQ', collection: 'requisitions' })
