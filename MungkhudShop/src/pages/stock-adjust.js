import { createDocumentPage } from './document-factory.js'
export const initStockAdjustPage = createDocumentPage({ title: 'ปรับสต็อก (SA)', icon: 'tune', prefix: 'SA', collection: 'stock_adjustments' })
